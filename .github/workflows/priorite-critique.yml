steps:
      - name: Gestion Priorité et Mouvement Projet
        uses: actions/github-script@v7
        with:
          # On ajoute un token avec les permissions de projet
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // 1. Assigner et Commenter (Ton code original)
            await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              assignees: [context.repo.owner]
            });
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: "🚨 **Alerte :** Ticket marqué comme Critique. Assignation effectuée et déplacement vers 'Todo'."
            });

            // 2. Déplacement dans le Projet (GraphQL)
            // Note: Remplace 'Suivi Issues' par le nom exact de ton projet si nécessaire
            const query = `
              mutation($project:ID!, $item:ID!, $status_field:ID!, $status_value:String!) {
                updateProjectV2ItemFieldValue(input: {
                  projectId: $project
                  itemId: $item
                  fieldId: $status_field
                  value: { singleSelectOptionId: $status_value }
                }) { projectV2Item { id } }
              }`;
            
            // Cette partie nécessite une configuration plus poussée des IDs.
            // Si c'est trop complexe, voici l'alternative simple ci-dessous.
